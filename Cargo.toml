[package]
name = "asm-bib"
version = "2.0.0"
edition = "2021"
description = "ASM-BIB — x86 ASM abstraction (NASM + MASM) with Python-like syntax"
authors = ["Eddi Andreé Salazar Matos"]

[dependencies]
